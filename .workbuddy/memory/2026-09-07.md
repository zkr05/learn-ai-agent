# 2026-09-07

- 用户称呼确认为 **kk**（已写入 ~/.workbuddy/MEMORY.md 与 USER.md），GitHub ID：zkr05。
- README 重写为求职作品集版：技能清单 Stage 0-7 + 项目亮点（面试可讲），提交 `8c07329`。
- 新建 `study-review-agent/`：Stage 5 的 study-review skill 升级为 production agent（Stage 7 Harness 设计落地）。
  - review_agent.py：tool registry（5 个采集工具，错误作为数据）、local/cloud 双后端 + auto fallback、结构不完整带反馈重试、run_log.jsonl 遥测、token 记账、--eval（4 用例含故意挂的 demo）。
  - .github/workflows/weekly-review.yml：每周一北京时间 10:00 自动跑，报告自动 commit 回仓库。
  - 本地验证通过：qwen2.5:7b 生成 2026-W37 报告（27s，$0），eval 4/4。
- 提交 `d3ff665`，连同积压 31 个提交一起推送到 origin/master。
- 待办：kk 需在 GitHub 仓库 Settings 配置 secret `LLM_API_KEY`（可选 vars LLM_BASE_URL/LLM_MODEL），Actions 才能在云端跑通。
- venv：openai 装在 C:\Users\admin\.workbuddy\binaries\python\envs\default。
